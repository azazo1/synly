# AGENTS

synly 在 android/ 下有实现, 同时支持 windows, macos, android.
设计新功能, 修复功能的时候, 请同时考虑它们.

android 构建可以使用:

```shell
just android-build
```

如果需要使用 gradlew 并且自动发现当前设备中的合适 android 环境, 请使用 `gradlew` recipe.
